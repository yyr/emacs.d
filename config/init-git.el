;; -----------------------------------------------------------------------------
;; Git (magit)
;; -----------------------------------------------------------------------------

(require 'magit) 
(autoload 'magit-status "magit" nil t)

(require 'rebase-mode)

(provide 'init-git)
